# Generated / non-source — Prettier 3 does not read .gitignore, so list them.
coverage/
dist/
node_modules/
pnpm-lock.yaml

# Generated by release-please; hand-formatting it would be reverted on the next
# release PR.
CHANGELOG.md

# Emitted by `pnpm schema:write` from src/core/config/schema.ts. Prettier
# collapses short arrays that JSON.stringify expands, so letting it format
# this file would leave it permanently at odds with `pnpm check:schema`.
schema/
